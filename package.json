{
    "name": "msmmer-root",
    "version": "1.0.0",
    "scripts": {
        "start": "npm run start --prefix server",
        "build": "npm install --prefix server && npm run build --prefix server && npm run prisma:generate --prefix server"
    }
}